import styled from 'styled-components';
import Img from '../Img';

const StyledLogo = styled(Img)`
  width: 121px;
  margin-left: 100px;
  position: relative;
  margin-top: 40px;
`;

export default StyledLogo;
