import styled from 'styled-components';
import ButtonSubscribe from './ButtonSubscribe';

const StyledButtonSubscribe = styled(ButtonSubscribe)`
  margin-left: 20px;
  cursor: pointer;
  color: #1890ff;

  &:hover{
    color: #40a9ff;
  }
`;

export default StyledButtonSubscribe;
